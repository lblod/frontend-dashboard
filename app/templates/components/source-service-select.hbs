{{#power-select
     placeholder="Acmidm login service, ..."
     search=(perform search)
     searchPlaceholder="Type to search..."
     options=options
     selected=selected
     onchange=(action "changeSelected")
     allowClear=true
     as |logEntry|}}
  {{last-part-uri logEntry.source}}
{{/power-select}}

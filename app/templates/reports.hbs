<div class="grid">
  <div class="col--3-12">
    {{#each model as |report|}}
      <LinkTo @route="reports.report" @model={{report.id}} class="vi-u-link">
        <h2>{{report.id}}</h2>
      </LinkTo>
    {{/each}}
  </div>
  <div class="col--9-12">
    {{outlet}}
  </div>
</div>